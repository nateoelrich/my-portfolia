/**
 * Animation constants used throughout the application
 */

/** Threshold for IntersectionObserver to trigger animations (0-1) */
export const ANIMATION_THRESHOLD = 0.25;

/** Delay between staggered tech stack item animations (in seconds) */
export const TECH_STACK_ANIMATION_DELAY = 0.05;
